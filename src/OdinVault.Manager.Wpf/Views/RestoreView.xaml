<UserControl x:Class="OdinVault.Manager.Wpf.Views.RestoreView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" FlowDirection="RightToLeft"><Grid Margin="26"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><TextBlock Text="{DynamicResource PageRestore}" FontSize="28" FontWeight="Bold" Margin="6,0,6,18"/><Border Grid.Row="1" Style="{StaticResource Card}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Margin="6"><TextBlock Text="شناسه بکاپ"/><TextBox Text="{Binding BackupIdText,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TechnicalText}"/></StackPanel><StackPanel Grid.Column="1" Margin="6"><TextBlock Text="نام دیتابیس مقصد"/><TextBox Text="{Binding Target,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TechnicalText}"/></StackPanel><Button Grid.Column="2" Content="بررسی اولیه" Click="Preflight_Click" VerticalAlignment="Bottom" IsEnabled="{Binding Running,Converter={StaticResource InverseBool}}"/></Grid></Border><Border Grid.Row="2" Style="{StaticResource Card}"><DockPanel><StackPanel DockPanel.Dock="Top"><TextBlock Text="{Binding Status}" TextWrapping="Wrap" Margin="0,0,0,12"/><Button Content="تأیید و اجرای بازیابی" Click="Restore_Click" IsEnabled="{Binding Ready}" HorizontalAlignment="Right"/></StackPanel><DataGrid ItemsSource="{Binding Files}" AutoGenerateColumns="False" IsReadOnly="True"><DataGrid.Columns><DataGridTextColumn Header="نام منطقی" Binding="{Binding LogicalName}" Width="*"/><DataGridTextColumn Header="نوع" Binding="{Binding Type}" Width="80"/><DataGridTextColumn Header="مسیر مقصد" Binding="{Binding Path}" Width="2*"/></DataGrid.Columns></DataGrid></DockPanel></Border></Grid></UserControl>